services:

  db:
    image: mysql:8
    container_name: kinoxp-db
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: kinoxp
    ports:
      - "3307:3306"

  app:
    build: .
    container_name: kinoxp-app
    ports:
      - "8080:8080"
    depends_on:
      - db
